<%= form_for :list_item, url: todo_list_list_items_path(@todo_list), remote: true do |f| %>
  <%= f.text_field :name %>
  <%= f.submit %>
<% end %>

<ul id="list_items">
  <%= render partial: "list_items/list_item", collection: list_items %>
</ul>
